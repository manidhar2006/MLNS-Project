Bundled KEGG knowledge artifacts (no REST cache).

Primary file used by arms B/C/D: tb_knowledge_graph_full.json

To regenerate from KEGG (network required), use the project-root script
kegg_fetch_all.py (or the copy under scripts/ in paper/mutgat_code); that
writes cache/ under kegg_data/ at the run location.
